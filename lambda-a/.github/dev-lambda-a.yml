name: dev-lambda-a@CI/CD
on:
  push:
    branches:
      - development
jobs:
  dev-lambda-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: echo 'running dev lambda-a'