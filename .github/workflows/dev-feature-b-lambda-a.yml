name: dev-feature-b-lambda-a@CI/CD
on:
  push:
    branches:
      - development
    paths: 
      - feature-b/lambda-a/**
jobs:
  dev-feature-b-lambda-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: echo 'running development feature-b lambda-a'